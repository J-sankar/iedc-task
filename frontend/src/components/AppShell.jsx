export default function AppShell({ user, view, setView, onSignOut, notice, children }) {
  return <main className="app-shell"><header className="topbar"><div className="brand"><span className="brand-mark">I</span><span>IEDC <b>venture desk</b></span></div><div className="account"><span>{user?.name || user?.email}</span><button className="text-button" onClick={onSignOut}>Sign out</button></div></header><nav className="nav-tabs">{[['overview', 'Overview'], ['create', 'Create project'], ['my-projects', 'My projects']].map(([key, label]) => <button className={view === key ? 'active' : ''} key={key} onClick={() => setView(key)}>{label}</button>)}</nav><div className="notice-area">{notice.error && <p className="notice error">{notice.error}</p>}{notice.message && <p className="notice success">{notice.message}</p>}</div>{children}</main>
}

import { STATUSES } from '../lib/api'

export default function CreateProject({ form, setForm, onSubmit, loading }) {
  return <section className="form-page"><p className="eyebrow">Project management / create</p><h1>Register a project.</h1><p className="muted">Create a new initiative. It will belong to your account.</p><form onSubmit={onSubmit} className="project-form"><label>Project title<input required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} placeholder="e.g. Campus Cart" /></label><label>Domain<input required value={form.domain} onChange={(event) => setForm({ ...form, domain: event.target.value })} placeholder="e.g. Climate tech" /></label><label>Abstract<textarea required rows="7" value={form.abstract} onChange={(event) => setForm({ ...form, abstract: event.target.value })} placeholder="What are you building, and why does it matter?" /></label><label>Initial status<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })}>{STATUSES.map((status) => <option key={status}>{status}</option>)}</select></label><button className="primary-button" disabled={loading}>{loading ? 'Creating...' : 'Create project'} <span>+</span></button></form></section>
}
